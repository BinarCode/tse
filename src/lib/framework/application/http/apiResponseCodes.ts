/**
 * This could be used for informative messages,
 * when in the frontend we should to notify user with specific backend message,
 * this would be helpful for the internationalization
 * @type {{info: number, warning: number}}
 */
export const apiResponseCodes = {
    info: 104,
    warning: 105,
    ok: 200,
    created: 201,
};
